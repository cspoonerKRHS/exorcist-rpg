**The avatar moved by the player.  There are now animations and 4 direction movement.**

# Variables #
  * screenSize
  * speed
  * pos (position of the player)

# Functions #
  * direction(dir)
  * move()
  * place(pt)
  * blockCollide(other)
  * distToPoint(pt)
  * wallCollide()
  * update(other)